package dev.cl0ud9.manager

import org.junit.Assert.assertEquals
import org.junit.Test

// keeps the test source set + CI test task wired until real unit tests land
class PlaceholderTest {
    @Test
    fun placeholder() {
        assertEquals(4, 2 + 2)
    }
}

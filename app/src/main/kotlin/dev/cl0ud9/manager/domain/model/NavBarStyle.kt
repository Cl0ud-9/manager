package dev.cl0ud9.manager.domain.model

// bottom navigation bar treatment, backing the nav-bar-style selector added to Settings > Appearance
enum class NavBarStyle {
    FLOATING_PILL,
    FULL_WIDTH,
}
